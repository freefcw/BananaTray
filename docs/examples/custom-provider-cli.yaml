# 示例：CLI 类型的自定义 Provider
#
# 将此文件放到 BananaTray 自定义 Provider 目录即可生效：
# - macOS: ~/Library/Application Support/BananaTray/providers/
# - Linux: ~/.config/bananatray/providers/
# 文件名任意，只要扩展名是 .yaml 或 .yml。

id: "myai:cli"

metadata:
  display_name: "My AI Tool"
  brand_name: "MyCompany"
  # icon: "C"                     # 可选，不配则自动取 display_name 首字母
  dashboard_url: "https://myai.com/usage"
  account_hint: "MyAI account"
  source_label: "myai cli"

# 可用性检查：检测 CLI 是否安装
availability:
  type: cli_exists
  value: "myai"

# 数据获取：执行 CLI 命令
source:
  type: cli
  command: "myai"
  args: ["usage", "--no-color"]

# 响应解析：用正则表达式提取配额数据
parser:
  format: regex
  # 从输出中提取账户邮箱（可选）
  account_email: 'Signed in as\s+(\S+)'
  quotas:
    - label: "Monthly Credits"
      pattern: 'Credits:\s*\$([0-9.]+)\s*/\s*\$([0-9.]+)'
      used_group: 1
      limit_group: 2
      quota_type: credit
